title: "Repository"
type: metrics
streams:
  - input: httpjson
    title: Repository metrics
    description: Collect repository metrics from IPFS node
    vars:
      - name: period
        type: text
        title: Period
        default: 10s
