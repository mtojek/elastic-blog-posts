title: "Application logs"
type: logs
streams:
  - input: logfile
    title: IPFS node application logs
    description: Collect IPFS node application logs
    vars:
      - name: paths
        type: text
        title: Paths
        multi: true
        default:
          - /var/log/ipfs-node-*.log
